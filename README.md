# Lua Nginx Edge Backend

Lua modules for OpenResty/ngx_lua: WAF rules, rate limiting, JWT validation,
and request sanitization. Includes a Python reference port for CI testing
since Lua runtime may not be available.

Run tests: `python -m pytest tests/`